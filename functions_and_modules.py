#1. Write functions for basic arithmetic operations (add, subtract, multiply, divide) and call them from a main function.
#2. Create a module containing a utility function (e.g., a temperature converter) and import it into another script.
#3. Write a program where the computer generates a random number, and the user has to guess it.
